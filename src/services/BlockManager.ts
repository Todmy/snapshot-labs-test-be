
class BlockManager {
  /**
   * searchBlockByTimestamp
   */
  public searchBlockByTimestamp(timestamp: number) {
    return { number: 1, hash: 'ttt' };
  }
}

const blockManager = new BlockManager();

export default blockManager;